$(function(){
	$.ajax({
		type : 'POST',
		url : 'SearchHouseServlet',
		data : {
			type : 'street',
			name : name
			
		},
		success : function(msg) {
			for(var i=0;i<pro.length;i++){
				var p=pro[i];
				var data=pro[i].split(",");
				$("#street").append("<option value="+data[1]+">"+data[0]+"</option>);")
			}
			
		}

	});
	$.ajax({
		type : 'POST',
		url : 'SearchHouseServlet',
		data : {
			type : 'types',
			name : name
			
		},
		success : function(msg) {
			//id=housetype
		}

	});
});